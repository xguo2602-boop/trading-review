---
name: trading-review
description: A股交易复盘体系。当用户发送每日/盘中复盘、大V（向阳/派大星）观点、操作记录、持仓更新、盘中询问或对照账户相关请求时使用。覆盖：复盘 8 问、两轴评分、大V 流水与命中记录表维护、风控红线与情绪三问、事件日历、对照账户（纸面）更新、数据查证规则。
---

# 交易复盘体系

## 核心原则

- **实事求是 = 最高元规则**：数据先行、承认不确定、错了就认；规则让位于事实但只走流程
- **主动维护**：命中记录表、条件单核对、对照账户更新必须主动做，不等用户提醒（漏做=违规）
- **数据查证**：不脑测；每个价格标注来源（用户报价✅/官方收盘✅/搜索⚠️/待确认❌），算不出就不给
- **结合当前行情**：不照本宣科套模板，每条分析落到用户具体持仓和当前盘面

## 工作流（先判断消息类型，再走对应流程）

### 0. 流程门（硬性，先声明后动手）

- 每条消息动手前，commentary 第一行必须写：**【流程门】类型=XX → 走YY流程**
- 类型枚举：晚间大篇复盘 / 午评早评盘中 / 操作账户 / 盘中情绪 / 问答
- 全流程/轻流程收尾必须落盘（大V流水、历史归档、命中记录、§2条件单、明日清单）；问答类只有声明行，答完即闭环
- 漏声明 / 声明了不落盘 / 类型标错 = 违规，记执行记录仪，月底大检核对
- 仅当用户明确说"不用走流程"才可跳过，且回复里写明原因

### 0.5 DB 同步铁律（写进 DB 才算数，漏同步=违规）

数据库 = 唯一事实源（可查、可统计）；MD 只是给人看的视图。结构化事实必须同步 DB，不等收盘：

| 动作 | 必须写进 DB 的表 |
|------|-----------------|
| 买卖/入金 | `transactions`（偏差/违规另写 `rule_triggers`） |
| 买卖决策 | `decision_cards`（先填卡后下单；**软删除**：status=已撤销+原因，不物理删） |
| 条件单挂/撤/触发/标的已清仓 | `condition_orders` 状态实时更新（卖出后检查对应单是否该撤销） |
| 大V/Codex 可验证观点 | `hits`（date=发布日期；verified_date=验证日；result=✅/❌/🟡） |
| 每日收盘 | `prices_snapshot`（自动；缺失=手动补并查原因） |
| 持仓 | `positions`（成本统一券商摊薄口径） |
| 复盘完成标记 | `reviews`（date/account/completed；8问正文叙事留 MD） |

- **🟡 老化**：超 7 个交易日未收敛 → audit 只出提醒清单，**不自动转 ❌**（避免冤枉中期观点），月底统计时人工判定
- **关键数字必须带来源标注**（用户报价✅ / 券商✅ / 行情接口✅），算不出就不给
- **编码纪律（Windows）**：带中文的 DB 写入/脚本一律用 apply_patch 写文件再跑、或命令行参数/环境变量传中文；**禁止 PowerShell 管道内嵌中文**（管道按系统代码页 GBK 传字节，python 按 UTF-8 解码会把汉字损坏成 `?`；历史踩坑 4 次，见历史归档第一百四十一次）
- **Schema 迁移纪律**：改表结构（ALTER/CREATE）前，先向 `schema_migrations` 写一条（version+日期+描述+SQL）再执行；已有 v0-v3 记录
- **隐私**：备份/上传 skill 只传规则文件，不传 portfolio.db 和复盘 MD
- **对账**：每周小检第 0 步跑 `audit.py`（快照缺失/卡未验证/条件单过期/🟡 老化/MD↔DB 差异），缺口当场补

### 1. 判断消息类型

| 类型 | 处理 |
|------|------|
| 晚间大篇复盘 | 全流程（5 步拆解） |
| 午评/早评/盘中提醒 | 轻流程但重质量：具体分析+独立判断，关键观点入流水+命中记录 |
| 操作/账户 | 两轴评分 + 数据核验 + 更新脚本配置 |
| 盘中情绪信号（"感觉不妙"等） | 先查数据再回应，不顺着情绪走 |
| 问答 | 直接答给依据 |

### 2. 晚间大篇全流程（5 步）

1. **客观事实**：他做了什么/仓位变化/说了什么
2. **逻辑归因**：基于什么信号/框架
3. **条件与边界**：什么条件下会改变决策，条件触发了吗
4. **自我映射**：他的资金/仓位/风格与我匹配吗，不匹配调整什么
5. **独立评判 + 风险评估**：漏洞/被忽略的风险，最坏结果能接受吗

之后：验证 → 入档（大V流水/历史归档）→ 命中记录表更新 → 行动清单

### 3. 每日复盘 8 问

每个交易日收盘后必须完成，见 [review-8q.md](references/review-8q.md)。未完成=违规。

### 4. 操作处理

- 两轴评分（过程分/结果分），见 [two-axis.md](references/two-axis.md)
- 操作后：更新 stock-plan §1/§2、portfolio_updater.py 配置、历史归档

### 5. 数据查证

见 [data-rules.md](references/data-rules.md)。禁止用搜索快照冒充实时行情。

### 6. 大V 追踪

逐日流水 + 命中记录表维护，见 [bigv-tracking.md](references/bigv-tracking.md)。

### 7. 风控与情绪拦截

用户想买/想卖/手痒时，先过 [risk-rules.md](references/risk-rules.md)：硬规则 6 条、情绪三问、禁买清单、伤疤股。

### 8. 周检/月检

周五小检、月底大检（规则修改权打开），见 [weekly-monthly.md](references/weekly-monthly.md)。

### 9. 对照账户（纸面）

每天检查待执行清单、记录操作，见 [sim-account.md](references/sim-account.md)。

### 10. 事件日历

财报/CPI/发射等提前写剧本，见 [event-calendar.md](references/event-calendar.md)。

### 11. 候选池与方法论

候选池触发价、值博率四象限、消息反馈法，见 [candidates.md](references/candidates.md)。

### 12. 决策预检卡（三行卡）

任何买卖决策前先填卡（先填卡后下单），收盘只验证不补卡。卡必须来自用户主动决策；验证点不预测价格；成交价必须实时（防后视镜）。完整规则与命令见 [decision-cards.md](references/decision-cards.md)。

## 档案位置

- 主文件：stock-plan.md（§0 速览/§1 持仓/§2 条件单/§4 规则/§8 事件日历）
- 附属：大V流水.md、历史归档.md、方法论文档.md、对照账户.md、持仓日报.md（由 portfolio_updater.py 生成）
- 脚本：portfolio_updater.py（腾讯行情 API，午盘 11:35/收盘 15:05 定时更新）、card.py（决策预检卡读写）、report.py（数据库日报）
- 数据库：portfolio.db（positions/transactions/hits[含 verified_date]/rule_triggers/condition_orders/prices_snapshot/decision_cards[含 note]/reviews/schema_migrations + backup/ 每日备份）
- 完整路径在对话上下文中，或按用户提供为准
